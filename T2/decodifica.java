
public class Decodifica {
/*
    public void type_R(String hex){

        String[] cut = hex.split("x");
        String hexAux = cut[1];
        
        char[] aux = hexAux.toCharArray();
        f d a 
        a.toBinaryString = string

        // ** UTILIZAR SUBSTRING ** 



        //String teste = aux[2]; 

        /*
        char[] aux = hex.toCharArray();
        System.out.println(aux[2]);
        /String bin = Integer.toBinaryString(aux[2]);

        String teste = Integer.toBinaryString(2);
        while(teste.length() < 4) teste = "0" + teste; 
        System.out.println("teste:" + teste);

        
        //Ridículo: achar outra forma de fazer
        String dois = Integer.toBinaryString(aux[2]);
        String tres = Integer.toBinaryString(aux[3]);
        String qua = Integer.toBinaryString(aux[4]);
        String cin = Integer.toBinaryString(aux[5]); 
        String seis = Integer.toBinaryString(aux[6]);
        String set = Integer.toBinaryString(aux[7]);
        String oit = Integer.toBinaryString(aux[8]);
        String nov = Integer.toBinaryString(aux[9]);

        while(dois.length() < 5) dois = "0" + dois;
        while(tres.length() < 5) tres = "0" + tres;
        while(qua.length() < 5) qua = "0" + qua;
        while(cin.length() < 5) cin = "0" + cin;
        while(seis.length() < 5) seis = "0" + seis;
        while(set.length() < 5) set = "0" + set;
        while(oit.length() < 5) oit = "0" + oit;
        while(nov.length() < 5) nov = "0" + nov;

        System.out.println("dois:" + dois );
        System.out.println("tres: "+tres);
        System.out.println("quatro: "+qua);
        System.out.println("cinco: "+cin);
        System.out.println("seis: "+seis);
        System.out.println("sete: "+set);
        System.out.println("oito: "+oit);
        System.out.println("nove: "+nov);
        */

        /*
        for(int i=2; i<aux.length-1; i++){
            //System.out.println(aux[i]);
            bin += Integer.toBinaryString(aux[i]);
           
        }*/
        //System.out.println("bin: " + bin);
}
